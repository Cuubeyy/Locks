package de.cubedude.locks.listeners;

import de.cubedude.locks.Main;
import de.cubedude.locks.utils.ConfigManager;
import org.bukkit.event.Listener;

public class LockedOpeningListener implements Listener {
    public LockedOpeningListener(ConfigManager config, Main main) {

    }
}
